# ========= SkyDeckPro (Frontend) =========
# Salin file ini menjadi `.env.local` lalu isi NILAI SEBENARNYA.
# Semua variabel di sini TER-EKSPOS ke browser (bukan rahasia).

# --- Supabase (frontend) ---
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_JWT
VITE_STORAGE_BUCKET=quiz-images

# --- Midtrans (frontend safe) ---
VITE_MIDTRANS_CLIENT_KEY=YOUR_MIDTRANS_CLIENT_KEY

# --- Base URL (dev) ---
# Saat develop dengan `netlify dev -p 8888`
VITE_PUBLIC_BASE_URL=http://localhost:8888

# --- Admin UI helper (opsional, bukan rahasia) ---
# Hanya untuk menampilkan beberapa fitur admin di UI.
# Boleh kosong jika selalu pakai Bearer token admin.
