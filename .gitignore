# ───── Logs ─────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ───── Dependencies & Build ─────
node_modules
dist
dist-ssr
.cache
.parcel-cache
.turbo
*.tgz
*.tar.gz

# ───── Environment files ─────
# ignore all env files, but keep example for developers
.env
.env.*
!.env.example

# if you want to keep local-only file name explicit, you can also create:
# .env.local (it will still be ignored by the rules above)

# ───── Netlify ─────
.netlify
.netlify/state.json
# keep netlify.toml in repo

# ───── Editor Directories & Files ─────
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ───── Testing & Coverage ─────
coverage/
.nyc_output/
__mocks__/
__snapshots__/

# (Optional) If you actually commit tests, remove the lines below.
# src/components/__tests__/
# src/pages/__tests__/
# src/tests/

# ───── System Junk Files (Windows/Unix) ─────
nul
Thumbs.db
ehthumbs.db
desktop.ini
